# Production build config for the admin SPA.
# Points the deployed admin at the backend VM over HTTPS (REST + Socket.IO).
# No trailing slash, no /api suffix — see src/api/config.js.
VITE_API_BASE=https://34-93-133-182.sslip.io
